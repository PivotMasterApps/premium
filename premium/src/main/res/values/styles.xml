<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="MySplashLayout" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:background">@color/premium_background</item>
        <item name="android:colorBackground">@color/premium_background</item>
        <item name="android:windowBackground">@color/premium_background</item>
    </style>

    <style name="CommonTextAppearanceDark" parent="@style/TextAppearance.AppCompat">
        <item name="android:textColor">@color/premium_text_color</item>
        <item name="android:color">@color/premium_text_color</item>
    </style>

    <style name="RatingDialogTheme" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:background">@color/premium_background</item>
        <item name="android:colorBackground">@color/premium_background</item>
        <item name="android:windowBackground">@color/premium_background</item>
        <item name="android:textColor">@color/premium_text_color</item>
    </style>
</resources>